query Profile($steamid: ID) {
	profile(steamid: $steamid) {
		steamid
		name
		profilePicture
		publicProfile

		standardRating {
			mmr
			rank
			rankTier
		}

		turboRating {
			mmr
			rank
			rankTier
		}

		twitchName
		discordName
	}
}
