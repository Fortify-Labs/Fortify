query CurrentMatches($limit: Int, $offset: Int) {
	currentMatches(limit: $limit, offset: $offset) {
		id
		averageMMR

		created
		updated
		ended

		slots {
			matchSlotID
			slot
			user {
				steamid
				name
			}
		}
	}
}
