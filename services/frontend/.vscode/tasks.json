{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "npm",
			"script": "compile",
			"problemMatcher": [
				"$tsc"
			],
			"group": "build",
			"label": "npm: compile",
			"detail": "graphql-let && tsc -b && cross-env NODE_ENV=production next build"
		}
	]
}