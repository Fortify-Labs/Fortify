{
  "name": "kubernetes",
  "version": "1.0.0",
  "main": "main.js",
  "types": "main.ts",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "import": "cdk8s import",
    "synth": "cdk8s synth",
    "compile": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "build": "npm run compile && npm run test && npm run synth && npm run clean",
    "upgrade": "npm i cdk8s@latest cdk8s-cli@latest",
    "upgrade:next": "npm i cdk8s@next cdk8s-cli@next",
    "record": "npx jest --updateSnapshot",
    "sync-dotenv": "sync-dotenv",
    "clean": "rename ./jest.config.js ./jest.config.js.bac && rimraf *.d.ts && rimraf *.js && rimraf src/*.d.ts && rimraf src/*.js && rimraf src/**/*.d.ts && rimraf src/**/*.js && rename ./jest.config.js.bac ./jest.config.js"
  },
  "dependencies": {
    "cdk8s": "^0.33.0",
    "constructs": "^3.2.16",
    "dotenv": "^8.2.0",
    "rename-cli": "^6.2.1"
  },
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "@types/node": "^14.14.6",
    "cdk8s-cli": "^0.33.0",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "sync-dotenv": "^2.6.2",
    "ts-jest": "^26.4.3",
    "typescript": "^4.0.5"
  }
}
